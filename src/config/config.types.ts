export type EnvVariables = {
    DISCORD_TOKEN: string;
    DISCORD_CLIENT_ID: string;
    APP_ENV: string;
    API_KEY: string;
    PORT: number;
}
export type JsonConfig = {
    bggApiUrl: string;
}
